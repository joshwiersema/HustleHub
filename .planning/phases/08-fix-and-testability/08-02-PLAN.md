---
phase: 08-fix-and-testability
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - app/onboarding/index.tsx
  - app/onboarding/pick-hustle.tsx
  - app/onboarding/setup-business.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/jobs.tsx
  - app/(tabs)/clients.tsx
  - app/(tabs)/earnings.tsx
  - app/(tabs)/profile.tsx
autonomous: false
requirements: [TEST-03, TEST-04, TEST-05]

must_haves:
  truths:
    - "User can complete onboarding from welcome screen through pick-hustle, setup-business, and land on home tab"
    - "User can tap each of the 5 bottom tabs and each screen loads content without errors"
    - "User can navigate to nested screens (job-detail, toolkit, business-card, etc.) and back without crashes"
    - "User can refresh the browser and see previously entered data (stores hydrated from AsyncStorage persistence)"
  artifacts:
    - path: "app/onboarding/index.tsx"
      provides: "Welcome screen that renders on web"
    - path: "app/(tabs)/index.tsx"
      provides: "Home tab that renders on web with hydrated store data"
    - path: "app/(tabs)/jobs.tsx"
      provides: "Jobs tab that renders on web"
    - path: "app/(tabs)/clients.tsx"
      provides: "Clients tab that renders on web"
    - path: "app/(tabs)/earnings.tsx"
      provides: "Earnings tab that renders on web"
    - path: "app/(tabs)/profile.tsx"
      provides: "Profile tab that renders on web"
  key_links:
    - from: "app/onboarding/setup-business.tsx"
      to: "src/store/profileStore.ts"
      via: "setProfile call marking onboarded"
      pattern: "setProfile|markOnboarded"
    - from: "app/(tabs)/index.tsx"
      to: "src/store/profileStore.ts"
      via: "useProfileStore hook reading profile data"
      pattern: "useProfileStore"
    - from: "all tab screens"
      to: "Zustand persist with AsyncStorage"
      via: "persist middleware with createJSONStorage"
      pattern: "persist.*AsyncStorage"
---

<objective>
Verify and fix all screens so onboarding completes, all 5 tabs load, navigation works, and stores persist data across browser refresh.

Purpose: After Plan 01 fixes the white screen, this plan ensures the full user experience works — onboarding flow, tab navigation, nested screen navigation, and data persistence. Any remaining web-incompatible code in individual screens will be caught and fixed.

Output: All screens load on web. Onboarding works end-to-end. Data persists across refresh. App is fully navigable.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-fix-and-testability/08-CONTEXT.md
@.planning/phases/08-fix-and-testability/08-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and fix all screens for web compatibility, verify onboarding flow and tab navigation</name>
  <files>app/onboarding/index.tsx, app/onboarding/pick-hustle.tsx, app/onboarding/setup-business.tsx, app/(tabs)/index.tsx, app/(tabs)/jobs.tsx, app/(tabs)/clients.tsx, app/(tabs)/earnings.tsx, app/(tabs)/profile.tsx, app/toolkit.tsx, app/job-detail.tsx, app/ideas.tsx, app/name-generator.tsx, app/pricing-calculator.tsx</files>
  <read_first>
    - app/onboarding/index.tsx
    - app/onboarding/pick-hustle.tsx
    - app/onboarding/setup-business.tsx
    - app/(tabs)/index.tsx
    - app/(tabs)/jobs.tsx
    - app/(tabs)/clients.tsx
    - app/(tabs)/earnings.tsx
    - app/(tabs)/profile.tsx
    - app/toolkit.tsx
    - app/job-detail.tsx
    - app/ideas.tsx
    - app/name-generator.tsx
    - app/pricing-calculator.tsx
    - src/store/profileStore.ts
    - src/store/gameStore.ts
  </read_first>
  <action>
1. **Audit every screen file** for web-incompatible patterns. For each file listed in read_first, check for:
   - Top-level imports of native-only modules (expo-haptics, expo-sharing, expo-print, react-native-view-shot, react-native-confetti-cannon)
   - Usage of `Haptics.*` without Platform.OS guard
   - Usage of `Alert.alert()` — works on web but behavior differs; leave as-is (it falls back to window.confirm)
   - Direct usage of native-only RN components not in react-native-web (most are fine, but check for any)

2. **Fix any remaining web-incompatible code found in the audit.** Apply the same pattern used in Plan 01:
   - Replace top-level native-only imports with conditional dynamic imports guarded by `Platform.OS !== 'web'`
   - Guard haptics calls with `Platform.OS !== 'web'` checks
   - Ensure no screen crashes on web due to missing native modules

3. **Start the dev server and test each screen loads on web:**
   - Run `npx expo start --web` (or verify it is already running)
   - Navigate to http://localhost:8081
   - If onboarding has not been completed: verify the welcome screen renders, then test the full onboarding flow (welcome -> pick-hustle -> setup-business -> home tab)
   - If onboarding was already completed: clear AsyncStorage by running in browser console `localStorage.clear(); location.reload()` to reset to onboarding state, then test the flow
   - After completing onboarding, tap each of the 5 tabs: Home, Jobs, Clients, Earnings, Profile — verify each renders content without errors
   - Check browser console for any red errors (warnings are acceptable)

4. **If any screen fails to render on web:**
   - Read the error from either the ErrorBoundary overlay or browser console
   - Fix the specific issue (likely a native-only import or API call)
   - Re-test that screen

5. **Verify store persistence (TEST-05):**
   - After completing onboarding, note the business name and hustle type selected
   - Refresh the browser (F5 or Ctrl+R)
   - Verify the app shows the home tab (not onboarding again) — this proves profileStore hydrated correctly
   - Verify the home tab shows the correct business name — this proves the data persisted
   - If hydration fails (app shows onboarding after refresh), investigate AsyncStorage web implementation. On web, AsyncStorage uses localStorage. Check:
     - `localStorage.getItem('@hustlehub/profile')` in browser console should return JSON with profile data
     - If null, the persist middleware is not writing. Check that `createJSONStorage(() => AsyncStorage)` works on web.
     - Fix: If AsyncStorage web adapter is broken, temporarily patch to use localStorage directly for web

6. **Document all fixes made** in the summary with file paths and what was changed.
  </action>
  <verify>
    <automated>cd C:/Users/joshu/Documents/HustleHub && npx tsc --noEmit 2>&1 | tail -10 && echo "---TypeCheck---" && grep -rn "import \* as Haptics" app/ src/ --include="*.tsx" --include="*.ts" | grep -v "node_modules" | grep -v "// " || echo "No top-level Haptics imports found (GOOD)"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` passes with no errors (or only pre-existing type warnings)
    - No top-level `import * as Haptics from 'expo-haptics'` in any app/ or src/ file (grep returns empty)
    - No top-level import of react-native-confetti-cannon in any file outside node_modules
    - All 5 tab screens render content on web without console errors
    - Onboarding flow completes from welcome through setup to home tab
    - After browser refresh, app shows home tab (not onboarding) proving store hydration works
  </acceptance_criteria>
  <done>Every screen in the app renders on web. Onboarding completes. All tabs load. Stores hydrate and persist across refresh.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Full app verification on web preview</name>
  <files>none</files>
  <action>Present verification instructions to user and wait for confirmation that the complete app works on web preview.</action>
  <what-built>All screens audited and fixed for web compatibility. Onboarding flow, tab navigation, nested screen navigation, and store persistence verified by Claude. The app should be fully functional on web preview.</what-built>
  <how-to-verify>
    1. Run `npx expo start --web` if not already running
    2. Open http://localhost:8081 in browser
    3. **Test onboarding** (if needed, clear data: browser console -> `localStorage.clear(); location.reload()`):
       a. Welcome screen should show HustleHub branding
       b. Tap "Get Started" -> Pick a hustle type (e.g., Lawn Care)
       c. Enter a business name and proceed -> Should land on Home tab
    4. **Test all 5 tabs:** Tap Home, Jobs, Clients, Earnings, Profile — each should show content
    5. **Test navigation:** From Profile tab, tap Toolkit -> Should show toolkit hub. Go back. From Jobs tab, tap + to add a job.
    6. **Test persistence:** Refresh browser (F5). App should still show the home tab with your business name (NOT restart onboarding)
    7. **Check console:** Open DevTools (F12) -> Console tab. Report any red error messages.
  </how-to-verify>
  <verify>User confirms all 7 verification steps pass</verify>
  <acceptance_criteria>
    - User confirms onboarding completes from welcome through setup to home tab
    - User confirms all 5 tabs load and display content
    - User confirms nested navigation (toolkit, add job) works and goes back correctly
    - User confirms browser refresh preserves data (does not restart onboarding)
    - User confirms no red errors in browser DevTools console
  </acceptance_criteria>
  <done>User has verified the complete app works on web preview. Onboarding, navigation, and persistence all functional.</done>
  <resume-signal>Type "approved" if all tests pass, or describe what failed</resume-signal>
</task>

</tasks>

<verification>
1. Onboarding welcome screen renders at http://localhost:8081 (if not previously onboarded)
2. Onboarding flow completes: welcome -> pick-hustle -> setup-business -> home tab
3. All 5 tabs (Home, Jobs, Clients, Earnings, Profile) render content
4. Nested navigation (Toolkit, Job Detail, etc.) works without crashes
5. Browser refresh preserves app state (does not restart onboarding)
6. Browser console shows no red errors during normal app usage
</verification>

<success_criteria>
- Onboarding completes end-to-end on web preview
- All 5 tabs load and display correct content
- Navigation to any nested screen and back works
- Data persists across browser refresh (Zustand stores hydrate from AsyncStorage/localStorage)
- Browser console has no red runtime errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-fix-and-testability/08-02-SUMMARY.md`
</output>
