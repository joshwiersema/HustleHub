---
phase: 07-integration-verification
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified: []
autonomous: true
requirements:
  - DSGN-01
  - DSGN-02
  - DSGN-03
  - DSGN-04
  - DSGN-05
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
  - CLNT-01
  - CLNT-02
  - CLNT-03
  - CLNT-04
  - CLNT-05
  - JOBS-01
  - JOBS-02
  - JOBS-03
  - JOBS-04
  - JOBS-05
  - JOBS-06
  - JOBS-07
  - GAME-01
  - GAME-02
  - GAME-03
  - GAME-04
  - GAME-05
  - GAME-06
  - GAME-07
  - PYMT-01
  - PYMT-02
  - PYMT-03
  - EARN-01
  - EARN-02
  - EARN-03
  - EARN-04
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04
  - DASH-05
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - PROF-05
  - TOOL-01
  - TOOL-02
  - TOOL-03
  - TOOL-04
  - TOOL-05
  - TOOL-06
  - IDEA-01
  - IDEA-02
  - IDEA-03

must_haves:
  truths:
    - "TypeScript compiles with zero errors (npx tsc --noEmit passes)"
    - "Expo bundler runs without errors (npx expo export --platform ios succeeds)"
    - "All imports resolve to existing modules with valid exports"
  artifacts:
    - path: "tsconfig.json"
      provides: "TypeScript configuration"
      contains: "strict"
  key_links:
    - from: "app/**/*.tsx"
      to: "src/store/*.ts"
      via: "Zustand store imports"
      pattern: "from.*src/store/"
    - from: "app/**/*.tsx"
      to: "src/components/*.tsx"
      via: "component imports"
      pattern: "from.*src/components/"
    - from: "app/**/*.tsx"
      to: "src/utils/*.ts"
      via: "utility imports"
      pattern: "from.*src/utils/"
---

<objective>
Run TypeScript compiler and Expo bundler against the full codebase to catch and fix any remaining type errors, missing imports, or bundling issues. This plan runs AFTER Plan 01 fixes the known bugs, so it validates the entire integrated codebase.

Purpose: Final verification gate -- if tsc and expo export both pass, the app is structurally sound and ready for runtime testing.

Output: Clean TypeScript compilation and Expo bundle with zero errors.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-integration-verification/07-01-SUMMARY.md

<interfaces>
<!-- Project structure for the executor to understand import resolution -->

tsconfig.json extends expo/tsconfig.base with strict: true

Store exports (src/store/index.ts):
- useProfileStore, useClientsStore, useJobsStore, usePaymentsStore, useGameStore

Component exports (src/components/index.ts):
- Card, GradientButton, XPBar, StatCard, HustleBucksDisplay, BadgeIcon, EmptyState, ScreenHeader, StreakBadge, CelebrationProvider, useCelebration

Utility exports (src/utils/gamification.ts):
- checkBadges, getBadgeProgress, getXPForLevel, getTotalEarningsFromJobs

Utility exports (src/utils/dateHelpers.ts):
- parseDateString, getNextOccurrenceDate, formatDuration

Type exports (src/types/index.ts):
- HustleType, HustleTypeInfo, UserProfile, Client, Job, Payment, Badge, LevelInfo
- HUSTLE_TYPES, LEVELS, BADGES, PAYMENT_METHODS

App screen files:
- app/_layout.tsx, app/index.tsx
- app/(tabs)/_layout.tsx, app/(tabs)/index.tsx, app/(tabs)/jobs.tsx, app/(tabs)/clients.tsx, app/(tabs)/earnings.tsx, app/(tabs)/profile.tsx
- app/onboarding/_layout.tsx, app/onboarding/index.tsx, app/onboarding/pick-hustle.tsx, app/onboarding/setup-business.tsx
- app/toolkit.tsx, app/flyer-generator.tsx, app/business-card.tsx, app/name-generator.tsx, app/pricing-calculator.tsx, app/ideas.tsx, app/job-detail.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run TypeScript compilation and fix all type errors</name>
  <read_first>
    - tsconfig.json
    - src/types/index.ts
    - src/store/index.ts
    - src/components/index.ts
  </read_first>
  <files></files>
  <action>
    Run `npx tsc --noEmit` from the project root directory.

    If it exits with code 0 and no errors, this task is done.

    If errors are reported, fix them by reading the referenced files and making targeted corrections. Common error categories to expect:

    1. **Missing type exports**: A screen imports a type that does not exist in src/types/index.ts. Fix by adding the export or correcting the import name.

    2. **Strict null checks**: A value might be null/undefined but is used without a guard. Fix by adding nullish coalescing (??) or optional chaining (?.).

    3. **Import path errors**: A file imports from a path that no longer exists (e.g., storage.ts was deleted in Plan 01). Fix by removing the import or updating the path.

    4. **Type mismatches**: A prop expects type X but receives type Y. Fix by correcting the value or adjusting the type.

    5. **Unused imports**: TypeScript strict mode may flag unused imports. Fix by removing them.

    After each fix, re-run `npx tsc --noEmit` to confirm the error is resolved before moving to the next.

    Iterate until `npx tsc --noEmit` exits cleanly with zero errors.

    IMPORTANT: Do not change the tsconfig.json to weaken type checking (e.g., do not set strict: false, do not add skipLibCheck: true). Fix the actual code.
  </action>
  <verify>
    <automated>cd /c/Users/joshu/Documents/HustleHub && npx tsc --noEmit 2>&1; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - No error lines in the output
    - tsconfig.json still has `"strict": true`
  </acceptance_criteria>
  <done>
    TypeScript compiler reports zero errors across the entire codebase with strict mode enabled.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run Expo bundler verification and fix any bundle errors</name>
  <read_first>
    - package.json
    - app/_layout.tsx
    - app/(tabs)/_layout.tsx
  </read_first>
  <files></files>
  <action>
    Run `npx expo export --platform ios` from the project root directory.

    This command attempts to bundle the entire app for iOS. If it succeeds, the Metro bundler can resolve all imports and the app structure is valid.

    If it fails, read the error output carefully. Common issues:

    1. **Module not found**: An import path is wrong or a package is not installed. Fix by correcting the import or running `npx expo install {package}`.

    2. **Syntax error**: A file has invalid syntax. Fix the syntax.

    3. **Duplicate exports**: Two files export the same name. Fix by renaming or consolidating.

    4. **Metro resolver issues**: Expo Router expects specific file naming conventions. Verify all route files match the expected pattern.

    After each fix, re-run `npx expo export --platform ios` to verify.

    If the export creates a `dist/` directory, clean it up after verification:
    `rm -rf dist/`

    Iterate until the export command succeeds.

    IMPORTANT: If `npx expo export --platform ios` is slow or hangs, try `npx expo export --platform web` as a faster alternative that still validates the module graph. Both catch import resolution issues.
  </action>
  <verify>
    <automated>cd /c/Users/joshu/Documents/HustleHub && npx expo export --platform web 2>&1 | tail -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - Expo export command exits with code 0
    - No "error" or "Error" lines in the output (excluding deprecation warnings)
    - All route files are found and bundled
  </acceptance_criteria>
  <done>
    Expo bundler successfully exports the complete app. All imports resolve, all route files are valid, and the module graph is complete.
  </done>
</task>

</tasks>

<verification>
Final verification checklist:
1. `npx tsc --noEmit` passes with exit code 0
2. `npx expo export --platform ios` or `--platform web` passes with exit code 0
3. No remaining references to deleted storage.ts
4. All 5 Zustand stores are importable from src/store/index.ts
5. All component exports resolve from src/components/index.ts
</verification>

<success_criteria>
1. TypeScript compiles with zero errors under strict mode
2. Expo bundler exports without errors
3. All imports across all 17+ source files resolve correctly
4. No dead imports, missing exports, or type mismatches remain
</success_criteria>

<output>
After completion, create `.planning/phases/07-integration-verification/07-02-SUMMARY.md`
</output>
