---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - app/_layout.tsx
  - app/index.tsx
  - app/(tabs)/_layout.tsx
  - app.json
autonomous: true
requirements:
  - DSGN-01
  - DSGN-04
must_haves:
  truths:
    - "App launches with dark background #0A0A0F and no white flash"
    - "Root layout gates onboarding vs tabs using Stack.Protected guard"
    - "Root layout shows loading spinner while Zustand profileStore hydrates"
    - "Five-tab bottom navigation renders with correct icons and colors"
    - "Tab bar uses theme constants not inline hex values"
  artifacts:
    - path: "app/_layout.tsx"
      provides: "Root layout with Stack.Protected guard and hydration awareness"
      exports: ["default"]
      contains: "Stack.Protected"
    - path: "app/index.tsx"
      provides: "Root path redirect to tabs"
      exports: ["default"]
    - path: "app/(tabs)/_layout.tsx"
      provides: "5-tab bottom navigation with theme constants"
      exports: ["default"]
      contains: "Colors.bgCard"
  key_links:
    - from: "app/_layout.tsx"
      to: "src/store/profileStore.ts"
      via: "useProfileStore selector for isOnboarded"
      pattern: "useProfileStore.*isOnboarded"
    - from: "app/_layout.tsx"
      to: "src/store/profileStore.ts"
      via: "persist.hasHydrated() and onFinishHydration"
      pattern: "useProfileStore\\.persist"
    - from: "app/(tabs)/_layout.tsx"
      to: "src/constants/theme.ts"
      via: "Colors.bgCard instead of inline #141419"
      pattern: "Colors\\.bgCard"
---

<objective>
Refactor the root layout to use Expo Router v7 Stack.Protected guard pattern with Zustand hydration awareness, and fix the tab layout to use theme constants instead of inline hex values. This delivers the navigation shell that all future screens plug into.

Purpose: The existing root layout uses conditional Stack.Screen rendering which is fragile and does not handle deep links or back navigation correctly. The Stack.Protected guard is the official Expo Router SDK 55 pattern. The tab layout has inline hex colors that should reference theme constants for maintainability. The app.json config is already correct for dark mode but needs expo-system-ui in plugins for Android.

Output: Hydration-aware root layout with Stack.Protected, fixed tab layout, validated app.json.
</objective>

<execution_context>
@C:/Users/joshu/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/joshu/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<!-- Prior plan dependency -->
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (Zustand stores) -->

From src/store/profileStore.ts:
```typescript
export const useProfileStore = create<ProfileState>()(
  persist(
    (set, get) => ({
      profile: null as UserProfile | null,
      isOnboarded: false,
      setProfile: (profile: UserProfile) => void,
      updateProfile: (updates: Partial<UserProfile>) => void,
      markOnboarded: () => void,
      reset: () => void,
    }),
    { name: '@hustlehub/profile', storage: createJSONStorage(() => AsyncStorage) }
  )
);
// Hydration API: useProfileStore.persist.hasHydrated(), useProfileStore.persist.onFinishHydration(cb)
```

From src/constants/theme.ts:
```typescript
export const Colors = {
  bg: '#0A0A0F',
  bgCard: '#141419',
  bgCardHover: '#1A1A22',
  primary: '#00E676',
  textMuted: '#5A5A66',
  text: '#FFFFFF',
  border: '#2A2A35',
  // ... full palette
};
```

From src/store/index.ts:
```typescript
export { useProfileStore } from './profileStore';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor root layout to Stack.Protected with hydration awareness</name>
  <files>app/_layout.tsx, app/index.tsx</files>
  <read_first>
    - app/_layout.tsx
    - app/index.tsx
    - src/store/profileStore.ts
    - src/constants/theme.ts
    - .planning/phases/01-foundation/01-RESEARCH.md
  </read_first>
  <action>
**Rewrite `app/_layout.tsx`** to use the Stack.Protected guard pattern with Zustand hydration awareness. The file should:

1. Import `useProfileStore` from `../src/store/profileStore` (NOT from `../src/store/storage`)
2. Import `Stack` from `expo-router`, `StatusBar` from `expo-status-bar`
3. Import `Colors` from `../src/constants/theme`
4. Import `React`, `useEffect`, `useState` from `react`
5. Import `View`, `ActivityIndicator`, `StyleSheet` from `react-native`

The component logic:
- Read `isOnboarded` from `useProfileStore((s) => s.isOnboarded)`
- Track hydration state with local `useState(false)` called `hydrated`
- In a `useEffect`, subscribe to `useProfileStore.persist.onFinishHydration(() => setHydrated(true))` and check if already hydrated with `useProfileStore.persist.hasHydrated()`
- If `!hydrated`, render a full-screen loading view with `backgroundColor: Colors.bg` (#0A0A0F) and a green `ActivityIndicator` with `color={Colors.primary}`
- Once hydrated, render the Stack with Stack.Protected guards:
  - `<Stack.Protected guard={isOnboarded}>` wrapping `<Stack.Screen name="(tabs)" />`
  - `<Stack.Protected guard={!isOnboarded}>` wrapping `<Stack.Screen name="onboarding" />`
- Stack screenOptions: `headerShown: false`, `contentStyle: { backgroundColor: Colors.bg }`, `animation: 'fade'`
- Include `<StatusBar style="light" />` outside the Stack

Do NOT import from `../src/store/storage` -- that module is deprecated.

**Rewrite `app/index.tsx`** to be a simple redirect:
- Import `Redirect` from `expo-router`
- Export a default component that returns `<Redirect href="/(tabs)" />`
- This handles the root path case; the Stack.Protected guard in _layout.tsx handles the onboarding gate
- Remove all AsyncStorage imports and loading state logic
  </action>
  <verify>
    <automated>grep -q "Stack.Protected" app/_layout.tsx && grep -q "useProfileStore" app/_layout.tsx && grep -q "hasHydrated" app/_layout.tsx && grep -q "Colors.bg" app/_layout.tsx && ! grep -q "storage" app/_layout.tsx && grep -q "Redirect" app/index.tsx && ! grep -q "AsyncStorage" app/index.tsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/_layout.tsx contains `Stack.Protected` (not conditional Stack.Screen rendering)
    - app/_layout.tsx imports `useProfileStore` from `../src/store/profileStore`
    - app/_layout.tsx does NOT import from `../src/store/storage`
    - app/_layout.tsx contains `hasHydrated` and `onFinishHydration` for hydration detection
    - app/_layout.tsx loading view uses `backgroundColor: Colors.bg` (resolves to `#0A0A0F`)
    - app/_layout.tsx contains `StatusBar style="light"`
    - app/index.tsx contains `Redirect href="/(tabs)"`
    - app/index.tsx does NOT import AsyncStorage or isOnboardingComplete
  </acceptance_criteria>
  <done>Root layout uses Stack.Protected guard pattern with Zustand hydration-aware loading screen. Index.tsx is a simple redirect. No references to deprecated storage.ts remain in navigation files.</done>
</task>

<task type="auto">
  <name>Task 2: Fix tab layout theme constants and validate app.json dark mode config</name>
  <files>app/(tabs)/_layout.tsx, app.json</files>
  <read_first>
    - app/(tabs)/_layout.tsx
    - app.json
    - src/constants/theme.ts
  </read_first>
  <action>
**Fix `app/(tabs)/_layout.tsx`:**

Replace the inline hex `backgroundColor: '#141419'` in the `tabBarStyle` with `backgroundColor: Colors.bgCard`. The value is identical (`Colors.bgCard` = `'#141419'`) but using the constant ensures future theme changes propagate.

Verify these locked tab bar values are correct:
- Active tint: `Colors.primary` (resolves to `#00E676` -- green)
- Inactive tint: `Colors.textMuted` (resolves to `#5A5A66` -- muted)
- Background: `Colors.bgCard` (resolves to `#141419` -- dark)
- Labels: hidden (`tabBarShowLabel: false`, icons only)
- Border: `Colors.border` (resolves to `#2A2A35`)

Verify the 5 tabs are: Home (home/home-outline), Jobs (calendar/calendar-outline), Clients (people/people-outline), Earnings (cash/cash-outline), Profile (person/person-outline). These match the existing code and locked decisions.

**Validate `app.json`:**

Read app.json and confirm these values exist (they should already be correct based on existing code):
- `"userInterfaceStyle": "dark"` -- prevents system light mode override
- `"splash.backgroundColor": "#0A0A0F"` -- matches Colors.bg, no white flash
- `"scheme": "hustlehub"` -- deep linking scheme

Add `"expo-system-ui"` to the `plugins` array if not already present. This ensures Android respects the dark mode setting. The current plugins array only has `"expo-router"`. The result should be:
```json
"plugins": ["expo-router", "expo-system-ui"]
```

Do NOT change `backgroundColor` from `#0A0A0F` -- it must match `Colors.bg` exactly.
  </action>
  <verify>
    <automated>grep -q "Colors.bgCard" app/\(tabs\)/_layout.tsx && ! grep -q "'#141419'" app/\(tabs\)/_layout.tsx && grep -q "expo-system-ui" app.json && grep -q '"userInterfaceStyle": "dark"' app.json && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <acceptance_criteria>
    - app/(tabs)/_layout.tsx uses `Colors.bgCard` not `'#141419'` for tabBarStyle backgroundColor
    - app/(tabs)/_layout.tsx uses `Colors.primary` for activeTintColor (already correct)
    - app/(tabs)/_layout.tsx uses `Colors.textMuted` for inactiveTintColor (already correct)
    - app/(tabs)/_layout.tsx has `tabBarShowLabel: false` (already correct)
    - app/(tabs)/_layout.tsx defines exactly 5 tabs: Home, Jobs, Clients, Earnings, Profile
    - app.json has `"userInterfaceStyle": "dark"`
    - app.json has `"splash.backgroundColor": "#0A0A0F"`
    - app.json plugins array includes both `"expo-router"` and `"expo-system-ui"`
    - No inline hex color strings remain in app/(tabs)/_layout.tsx that match theme constants
  </acceptance_criteria>
  <done>Tab layout uses theme constants exclusively. app.json validated for dark mode with expo-system-ui plugin added for Android support. 5-tab navigation confirmed with correct icons and color assignments.</done>
</task>

</tasks>

<verification>
- `app/_layout.tsx` uses Stack.Protected (not conditional rendering)
- `app/_layout.tsx` imports from Zustand store (not storage.ts)
- `app/(tabs)/_layout.tsx` has no inline hex color strings
- `app.json` has dark mode config and expo-system-ui plugin
- No file imports from `src/store/storage.ts` in the `app/` directory
</verification>

<success_criteria>
The navigation shell is refactored to use Stack.Protected guard with hydration-aware loading. Tab layout uses theme constants exclusively. app.json is validated for dark mode with expo-system-ui. The onboarding gate works declaratively through Zustand profileStore isOnboarded state.
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
